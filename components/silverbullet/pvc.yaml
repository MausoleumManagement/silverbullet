apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: silverbullet-data
spec:
  storageClassName: cephfs-2026
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
